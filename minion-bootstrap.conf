# This file is only used during the Terraform provisioning process 
# to bootstrap management from the Gitfs state/pillar roots.

file_roots:
  mgmt1:
    - /srv/salt
pillar_roots:
  mgmt1:
    - /srv/salt/pillar